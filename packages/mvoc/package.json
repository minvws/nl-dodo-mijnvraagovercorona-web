{
	"name": "@quarantaine/mvoc",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "yarn run download-images && yarn run download-content-variables && next dev",
		"preview": "yarn run download-images && yarn run download-content-variables && next build",
		"build": "yarn run download-images && next build && next-sitemap && next export",
		"download-images": "node ./downloadSanityImages.js",
		"download-content-variables": "node ./downloadLatestContentVariables.js",
		"start": "NODE_ENV=production next start",
		"test": "true"
	},
	"browserslist": [
		"> 0.5% in NL",
		"last 2 versions",
		"Firefox ESR",
		"not dead",
		"edge <= 42"
	],
	"dependencies": {
		"date-fns": "^2.29.3",
		"next": "12.3.0",
		"next-sanity": "^0.8.4",
		"theme-ui": "^0.14.7"
	},
	"devDependencies": {
		"next-sitemap": "^3.1.22",
		"next-transpile-modules": "^9.0.0",
		"typescript": "^4.8.3"
	}
}
