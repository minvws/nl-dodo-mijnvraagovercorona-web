---
title: 'Link'
sidebar:
  order: 3
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Link } from '@lib/elements/Link';

`Links` are used for navigating. The base style is used in body text and general links to other pages. Additionaly a `Link` can have an icon which by default is placed after the label. With configurable options you can place the icon in front of the label. You can also pass the `variant` variable to style the `Link` to default presets.

## Usage

```jsx
import { Link } from '@dodo/ui/elements';

<Link href="#">Link label</Link>
```

## Base Link
<div class="not-content">
	Body text with a <Link href="#">link</Link> in it.
</div>

```jsx
<p>Body text with a <Link href="#">link</Link> in it.</p>
```

## External Link

<div class="not-content">
	Body text with a <Link href="#" isExternal>link</Link> in it.
</div>

```jsx
<p>Body text with a <Link href="#" isExternal>link</Link> in it.</p>
```

## Variants

<Tabs>
  <TabItem label="Tooltip">
		<div class="not-content">
			<Link href="#" variant="tooltip">Tooltip variant</Link>
		</div>
		```jsx
		<Link href="#" variant="tooltip">Tooltip variant</Link>
		```
	</TabItem>
	<TabItem label="Restart">
		<div class="not-content">
			<Link href="#" variant="restart">Restart variant</Link>
		</div>
		```jsx
		<Link href="#" variant="restart">Restart variant</Link>
		```
	</TabItem>
	<TabItem label="Back">
		<div class="not-content">
			<Link href="#" variant="back">Back variant</Link>
		</div>
		```jsx
		<Link href="#" variant="back">Back variant</Link>
		```
	</TabItem>
	<TabItem label="CTA">
		<div class="not-content">
			<Link href="#" variant="cta" icon="phone" iconPosition="leading">CTA variant</Link>
		</div>
		```jsx
		<Link href="#" variant="cta" icon="phone" iconPosition="leading">CTA variant</Link>
		```
	</TabItem>
</Tabs>

