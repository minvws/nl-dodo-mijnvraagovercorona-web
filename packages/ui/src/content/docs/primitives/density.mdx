---
title: 'Density'
sidebar:
  order: 3
---

import { Density } from '@components/Density';


`Density` is used to create space inside of components like `Layer` and `Hero`.

## Examples

Adjust the density of a component with a `--density-[size]` variable.

```css
.your-component {
	padding: var(--density-compact);
}
```

<div class="not-content">
  <Density/>
</div>
