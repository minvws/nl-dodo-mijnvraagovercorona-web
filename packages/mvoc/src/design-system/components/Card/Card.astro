---
import { HTMLAttributes } from 'astro/types';
interface Props extends HTMLAttributes<'div'> {
	variant?: 'base' | 'filled' | 'frost' | 'white' | 'canvas';
}

const { variant = 'base', class: className, ...attrs } = Astro.props;
---

<div
	class={`c-card${variant ? ` c-card--${variant}` : ''}${
		className ? ` | ${className}` : ''
	}`}
	{...attrs}
>
	<slot />
</div>

<style lang="scss">
	.c-card {
		--card-border-width: 1px;
		--card-border-color: var(--color-lichtblauw);
		padding: calc(var(--space-r-base) - var(--card-border-width));
		border-radius: var(--radii-tiny);
		border: var(--card-border-width) solid var(--card-border-color);
	}

	.c-card--filled {
		--card-border-width: 0px;
		background-color: var(--color-poolblauw);
		color: var(--color-diepblauw);
	}

	.c-card--frost {
		--card-border-width: 0px;
		background-image: var(--gradient-lichtblauw);
		box-shadow: var(--elevation-frost);
	}

	.c-card--white {
		--card-border-width: 0px;
		background-color: var(--color-white);
		color: var(--color-black);
	}

	.c-card--canvas {
		--card-border-width: 0px;
		background-color: var(--color-white);
		box-shadow: var(--elevation-frost);
		border-radius: var(--radii-nano);
	}
</style>
