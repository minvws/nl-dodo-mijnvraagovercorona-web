---
import { Block } from 'astro-portabletext/components';

export type Props = Parameters<typeof Block>[0];

const props = Astro.props;
const styleIs = (style: string) => style === props.node.style;
const Cmp = styleIs('span') ? 'span' : Block;
---

<Cmp {...props}><slot /></Cmp>
