{
	"name": "mvoc",
	"private": true,
	"scripts": {
		"test": "npm run test --workspaces",
		"mvoc:dev": "npm run dev --workspace mvoc",
		"mvoc:build": "npm run build --workspace mvoc",
		"mvoc:preview": "npm run preview --workspace mvoc",
		"mvoc:test": "npm run test --workspace mvoc",
		"cms:dev": "npm run dev --workspace cms",
		"cms:deploy": "npm run deploy --workspace cms",
		"cms:build": "npm run build-development --workspace cms",
		"format": "npm run format:prettier",
		"format:prettier": "prettier --write ."
	},
	"workspaces": [
		"./packages/*"
	],
	"dependencies": {
		"typescript": "^5.3.3"
	}
}
