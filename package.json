{
	"name": "quarantaine",
	"private": true,
	"scripts": {
		"test": "npm run test --workspaces",
		"mvoc:test": "npm run test --workspace mvoc",
		"mvoc:dev": "npm run dev --workspace mvoc",
		"mvoc:build": "npm run build --workspace mvoc",
		"mvoc:check-changed-documents:test": "npm run check-changed-documents:test --workspace mvoc",
		"mvoc:check-changed-documents:staging": "npm run check-changed-documents:staging --workspace mvoc",
		"mvoc:check-changed-documents:production": "npm run check-changed-documents:production --workspace mvoc",
		"cms:dev": "npm run dev --workspace cms",
		"cms:build-development": "npm run build-development --workspace cms",
		"cms:build-staging": "npm run build-staging --workspace cms",
		"cms:build-main": "npm run build-main --workspace cms",
		"format": "npm run format:prettier",
		"format:prettier": "prettier --write ."
	},
	"workspaces": [
		"./packages/*"
	],
	"devDependencies": {
		"vercel": "^28.1.0"
	}
}
