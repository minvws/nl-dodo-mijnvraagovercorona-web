import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { Logo } from './';

<Meta title="Components/Logo" component={Logo} />

# Logo

The logo component can be used to render the logo on the top of the page.

Make sure the logo itself is exported to the public folder.

## Props

<Props of={Logo} />

## Component usage

<Canvas>
	<Story name="Logo">
		<Logo alt="Logo voor deze website" />
	</Story>
</Canvas>
