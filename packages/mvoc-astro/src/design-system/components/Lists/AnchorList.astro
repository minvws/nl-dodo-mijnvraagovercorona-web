---
import { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'div'> {}

const { class: className, ...attrs } = Astro.props;
---

<div class={`classname${className ? ` | ${className}` : ''}`} {...attrs}>
	<slot />
</div>

<style lang="scss">
	.c-anchor-list {
		//code
	}
</style>
